{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StrikeManifest",
  "description": "The momentum-ready SVM strike request to the Liquidity Settlement Fabric.",
  "type": "object",
  "properties": {
    "payload": {
      "$ref": "./intent.json"
    },
    "mpc_signature": {
      "type": "string",
      "pattern": "^ed25519:[a-zA-Z0-9]{87,88}$",
      "description": "The Ed25519 cryptographic signature generated by the Near MPC threshold network. EVM signatures are strictly rejected."
    },
    "deadline": {
      "type": "integer",
      "description": "Unix timestamp for the settlement deadline."
    }
  },
  "required": ["payload", "mpc_signature", "deadline"]
}
