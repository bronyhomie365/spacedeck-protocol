# 💎 THE SOVEREIGN ALCHEMY PROTOCOL (SAP) V1
**CONTEXT:** Transmuting "Local Prototype Mess" -> "Pristine Production Vessel"

This protocol defines the recursive steps taken to elevate the **Spacedeck Protocol** from a fragmented prototype into an institutional-grade, deterministic monorepo. Use this as your master compass for all future MentalOS manifestations.

---

## 🌌 1. THE ONTOLOGICAL ALIGNMENT (THE SEPHIROTH)
We do not build apps; we manifest organisms. Every directory corresponds to a Sephirah on the Tree of Life:
- **00_Kether (Orchestration):** The initial "Will" and generator scripts.
- **09_Yesod (Foundation/API):** The Python Orchestrator managing the Hyper-Graph.
- **07_Netzach (Endurance/Rust):** The Kinetic Heart for deterministic execution.
- **10_Malkuth (The Kingdom/UI):** The Next.js frontend vessel.

> [!TIP]
> **Deterministic Transparency:** By naming your folders/services after these nodes, you ensure that any Agent (Human or AI) understands the "Soul" of the code instantly.

---

## 🛠️ 2. THE TRANSMUTATION PROCESS (THE 4 SHARDS)

### Shard 1: Structural Sublimation (The Scaffold)
- **Action:** Initializing a **Turborepo Monorepo** using workspace-based dependency management (npm/pnpm).
- **Rule:** NEVER "move" files to a new folder. ALWAYS "Inhale" them into a clean scaffold using a fresh `package.json` to shed legacy dependency bloat (Entropy).

### Shard 2: Physical Purification (The Asset Strike)
- **Action:** Migrating local/google fonts to `next/font/local`.
- **Target:** 0ms FOUT (Flash of Unstyled Text) and <1s Load Resonance.
- **Technique:** Centralize redundant UI logic (e.g., `<CrystallineBorder />`) to reduce JS payload weight.

### Shard 3: The Kinetic Heart (Dual-CORE Backend)
- **Logic:** Decouple the **Brain** (Python) from the **Muscle** (Rust).
- **Automation:** Implement a **Shadow Fallback** in the API. If the Rust Forge is cold/offline, the API must "Shadow Meditate" and return high-fidelity simulation telemetry to keep the user experience alive.

### Shard 4: The Omega Audit (Forensic Truth)
- **Manifest:** Create a `/tests` folder that mirrors the production geometry.
- **Goal:** Forensic Transparency. Every "Strike" (execution) must produce a entry in an `Omega_Audit.json` log.
- **Aesthetic:** High-fidelity CLI outputs during tests to wow judges/investors with project maturity.

---

## 🚀 3. THE DEPLOYMENT MANIFOLD (THE CLOUD)

| Node | Strategy | Logic |
| :--- | :--- | :--- |
| **Vercel** | Frontend (`apps/web`) | Automatic preview deployments & high-speed Edge caching. |
| **Fly.io / VPS**| Backend (`services/*`) | Running the Docker Equilibrium for containerized Rust/Python. |
| **GitHub** | Repository Secret Ops | Secrets isolated in Gevurah (environment vars), never committed. |

---

## ⚛️ 4. THE AUTOPILOT CLI COMMANDS
Reference these in your future `MentalOS` prompts:

1. **"Scaffold the Vessel":** Initializing the Turborepo/Workspace.
2. **"Perform the Asset Strike":** Optimizing fonts/assets for speed.
3. **"Manifest the Kinetic Heart":** Setting up the Python/Rust microservices.
4. **"Execute Omega-Scan":** Running the recursive test suite.

---
